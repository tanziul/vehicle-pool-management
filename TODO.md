# Vehicle Pool Management - Task Progress

## Current Task: Add Vehicle Photos to Booking Pages

### Completed Tasks:
- [x] Update `templates/admin/bookings.html` Vehicle column to include vehicle photo (same as user bookings)
- [x] Update `templates/profile.html` Recent Bookings Vehicle column to include vehicle photo (same as user bookings)
- [x] Update `templates/admin/dashboard.html` Recent Pending Requests Vehicle column to include vehicle photo (for consistency)
- [x] Update `templates/profile.html` Date & Time column format to match admin bookings page (Start • End format)
- [x] Update `templates/admin/dashboard.html` Start • End column format to match admin bookings page
- [x] Update `templates/admin/bookings.html` Employee column to use default profile pictures (initials-based avatars with onerror fallback)
- [x] Update `templates/admin/dashboard.html` Employee column to use default profile pictures (initials-based avatars with onerror fallback)

### Followup Steps:
- [ ] Test the changes to ensure photos display correctly and date/time format is consistent across all booking tables
